@if(Request.IsAuthenticated) {
    <text>Zapraszamy <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Wyloguj", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Zaloguj", "LogOn", "Account") ]
    @:[ @Html.ActionLink("Rejestracja", "Register", "Account") ]
}
